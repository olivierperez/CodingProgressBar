rootProject.name = "CodingProgressBar"

pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        //maven("https://oss.sonatype.org/content/repositories/snapshots/")
    }
}